pandas==2.2.2
numpy==1.26.4
PyYAML==6.0.1
requests==2.32.3
mplfinance==0.12.10b0
matplotlib==3.9.0

# State persistence uses `sqlite3`, which ships with the Python standard
# library — there is NO pip package to install. Do not add `sqlite3` here;
# it is not on PyPI and would make `pip install -r requirements.txt` fail.
